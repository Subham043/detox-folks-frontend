type ENV = {
    API_ENDPOINT: string;
    CURRENT_ENV: string;
}

export const ENV:ENV = {
    // API_ENDPOINT : "http://localhost:8000",
    API_ENDPOINT : "https://server-api.parcelcounter.in",
    CURRENT_ENV : "dev"
}